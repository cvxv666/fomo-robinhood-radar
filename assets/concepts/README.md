# Concepts

Pages that show what a thing would look like before it exists. Not products; nothing here is wired to the radar.

| file | what |
|---|---|
| `engine.html` | The execution engine on top of the radar: six gates in a lattice, three loops back, one book. Simulated signals run the gates live; `?still=1` for a populated frame. |
